{
  "name": "back",
  "private": true,
  "version": "1.0.0",
  "description": "Express API — deps at repo root so npm install always installs express, etc.",
  "scripts": {
    "start": "node backend/server.js",
    "dev": "node backend/server.js",
    "build": "npm install"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "connect-pg-simple": "^9.0.1",
    "express": "^4.18.2",
    "express-session": "^1.17.3",
    "pg": "^8.13.1"
  },
  "engines": {
    "node": ">=18"
  }
}
